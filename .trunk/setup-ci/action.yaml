name: Trunk Check setup
description: Set up dependencies for Trunk Check

runs:
  using: composite
  steps:
  - id: init
    name: Initialise Bit via SH
    shell: bash
    run: ./scripts/bit-init.sh
  - id: verify
    name: Verify and Build Bit
    shell: bash
    run: ./scripts/verify.sh

diff --git a/dist/cjs/internal/fiberRefs.js b/dist/cjs/internal/fiberRefs.js
index cecb9ea23a1e3df589b4f44a09d8ddc92c5fb87a..e696b84b80de59ac796e67c2fa925052b8e94974 100644
--- a/dist/cjs/internal/fiberRefs.js
+++ b/dist/cjs/internal/fiberRefs.js
@@ -77,6 +77,9 @@ const findAncestor = (_ref, _parentStack, _childStack, _childModified = false) =
 const joinAs = exports.joinAs = /*#__PURE__*/(0, _Function.dual)(3, (self, fiberId, that) => {
   const parentFiberRefs = new Map(self.locals);
   that.locals.forEach((childStack, fiberRef) => {
+    if (fiberRef == null || typeof fiberRef !== "object" || typeof fiberRef.initial === "undefined") {
+      return;
+    }
     const childValue = childStack[0][1];
     if (!childStack[0][0][Equal.symbol](fiberId)) {
       if (!parentFiberRefs.has(fiberRef)) {
diff --git a/dist/cjs/internal/fiberRuntime.js b/dist/cjs/internal/fiberRuntime.js
index 926ffb47eb8730023fcf984313201bd70405c40c..0cb5fbf69576d0de75630a6f18eb1d171c7e5438 100644
--- a/dist/cjs/internal/fiberRuntime.js
+++ b/dist/cjs/internal/fiberRuntime.js
@@ -418,9 +418,15 @@ class FiberRuntime extends Effectable.Class {
    * log annotations and log level) may not be up-to-date.
    */
   getFiberRef(fiberRef) {
+    if (fiberRef == null || typeof fiberRef !== "object") {
+      throw new Error("FiberRef is undefined or invalid - possible duplicate Effect instances. Ensure pnpm overrides effect to a single version.");
+    }
     if (this._fiberRefs.locals.has(fiberRef)) {
       return this._fiberRefs.locals.get(fiberRef)[0][1];
     }
+    if (typeof fiberRef.initial === "undefined") {
+      throw new Error("FiberRef.initial is undefined - FiberRef may be from a different Effect instance. Ensure single effect copy via pnpm overrides.");
+    }
     return fiberRef.initial;
   }
   /**
@@ -1149,7 +1155,14 @@ class FiberRuntime extends Effectable.Class {
         // @ts-expect-error
         cur = this.currentTracer.context(() => {
           if (_version !== cur[core.EffectTypeId]._V) {
-            const level = this.getFiberRef(core.currentVersionMismatchErrorLogLevel);
+            let level;
+            try {
+              level = core.currentVersionMismatchErrorLogLevel != null
+                ? this.getFiberRef(core.currentVersionMismatchErrorLogLevel)
+                : Option.none();
+            } catch {
+              level = Option.none();
+            }
             if (level._tag === "Some") {
               const effectVersion = cur[core.EffectTypeId]._V;
               this.log(`Executing an Effect versioned ${effectVersion} with a Runtime of version ${version.getCurrentVersion()}, you may want to dedupe the effect dependencies, you can use the language service plugin to detect this at compile time: https://github.com/Effect-TS/language-service`, internalCause.empty, level);
diff --git a/dist/esm/internal/fiberRefs.js b/dist/esm/internal/fiberRefs.js
index cd0f746726d326ba0af92437c53d0228dae24dd4..4f8516caea6a912e29e887f4428c04bfcec91de5 100644
--- a/dist/esm/internal/fiberRefs.js
+++ b/dist/esm/internal/fiberRefs.js
@@ -65,6 +65,9 @@ const findAncestor = (_ref, _parentStack, _childStack, _childModified = false) =
 export const joinAs = /*#__PURE__*/dual(3, (self, fiberId, that) => {
   const parentFiberRefs = new Map(self.locals);
   that.locals.forEach((childStack, fiberRef) => {
+    if (fiberRef == null || typeof fiberRef !== "object" || typeof fiberRef.initial === "undefined") {
+      return;
+    }
     const childValue = childStack[0][1];
     if (!childStack[0][0][Equal.symbol](fiberId)) {
       if (!parentFiberRefs.has(fiberRef)) {
diff --git a/dist/esm/internal/fiberRuntime.js b/dist/esm/internal/fiberRuntime.js
index b4b6b60da9f0b6b4cacd15c57354e941a9d2dd6e..93bbcf3412189c6e42fbfbde18dea1b27e50d817 100644
--- a/dist/esm/internal/fiberRuntime.js
+++ b/dist/esm/internal/fiberRuntime.js
@@ -410,9 +410,15 @@ export class FiberRuntime extends Effectable.Class {
    * log annotations and log level) may not be up-to-date.
    */
   getFiberRef(fiberRef) {
+    if (fiberRef == null || typeof fiberRef !== "object") {
+      throw new Error("FiberRef is undefined or invalid - possible duplicate Effect instances. Ensure pnpm overrides effect to a single version.");
+    }
     if (this._fiberRefs.locals.has(fiberRef)) {
       return this._fiberRefs.locals.get(fiberRef)[0][1];
     }
+    if (typeof fiberRef.initial === "undefined") {
+      throw new Error("FiberRef.initial is undefined - FiberRef may be from a different Effect instance. Ensure single effect copy via pnpm overrides.");
+    }
     return fiberRef.initial;
   }
   /**
@@ -1141,7 +1147,14 @@ export class FiberRuntime extends Effectable.Class {
         // @ts-expect-error
         cur = this.currentTracer.context(() => {
           if (_version !== cur[core.EffectTypeId]._V) {
-            const level = this.getFiberRef(core.currentVersionMismatchErrorLogLevel);
+            let level;
+            try {
+              level = core.currentVersionMismatchErrorLogLevel != null
+                ? this.getFiberRef(core.currentVersionMismatchErrorLogLevel)
+                : Option.none();
+            } catch {
+              level = Option.none();
+            }
             if (level._tag === "Some") {
               const effectVersion = cur[core.EffectTypeId]._V;
               this.log(`Executing an Effect versioned ${effectVersion} with a Runtime of version ${version.getCurrentVersion()}, you may want to dedupe the effect dependencies, you can use the language service plugin to detect this at compile time: https://github.com/Effect-TS/language-service`, internalCause.empty, level);

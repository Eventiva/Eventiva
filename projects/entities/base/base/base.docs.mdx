---
labels: ['base', 'entity']
description: 'A Base entity.'
---

A component for a Base entity, supports serialization and de-serialization of Base objects and can be used from
both backend and frontend.

## Get started

Install and use the entity to create a new instance:

```ts
const base = Base.from({
  name: 'hello'
});
```

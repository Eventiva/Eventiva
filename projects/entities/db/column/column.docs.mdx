---
labels: ['column', 'entity']
description: 'A Column entity.'
---

A component for a Column entity, supports serialization and de-serialization of Column objects and can be used from
both backend and frontend.

## Get started

Install and use the entity to create a new instance:

```ts
const column = Column.from({
  name: 'hello'
});
```

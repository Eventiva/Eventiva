---
labels: ['document', 'entity']
description: 'A Document entity.'
---

A component for a Document entity, supports serialization and de-serialization of Document objects and can be used from
both backend and frontend.

## Get started

Install and use the entity to create a new instance:

```ts
const document = Document.from({
  name: 'hello'
});
```

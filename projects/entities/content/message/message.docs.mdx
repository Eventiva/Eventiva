---
labels: ['message', 'entity']
description: 'A Message entity.'
---

A component for a Message entity, supports serialization and de-serialization of Message objects and can be used from
both backend and frontend.

## Get started

Install and use the entity to create a new instance:

```ts
const message = Message.from({
  name: 'hello'
});
```

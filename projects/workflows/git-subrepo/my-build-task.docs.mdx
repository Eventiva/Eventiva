---
labels: ['my build task', 'app', 'app type']
description: 'A MyBuildTask app component.'
---

A basic Bit app component.

## Run the app

```
# use the app
bit use my-build-task

# run the app
bit run my-build-task
```

## Compose the app

Compose the app to create your own instance of this app.

```ts
export default MyBuildTask.from({});
```

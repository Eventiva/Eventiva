---
labels: ['discord', 'runtime', 'harmony', 'aspects', 'composable']
description: 'A Discord runtime for Harmony platforms.'
---

A Discord runtime for Harmony. Use to allow your aspects in new runtimes:

## Get started

Use the Discord Runtime in your Harmony platform:

```ts
import { UIRuntime } from '@bitdev/harmony.runtimes.ui-runtime';

export const WayneCom = HarmonyPlatform.from({
  name: 'wayne-com',

  main: [WaynePlatformAspect],

  runtimes: [
    new UIRuntime()
  ],

  aspects: [
    PeopleAspect
  ]
});

```

Create an aspect, or add a `*.ui.runtime.ts` runtime to one of your existing aspects:

```
bit create aspect people
```


Run your platform:

```
bit run wayne-com
```
            
